const name = 'Alex';
const surname = 'Meruelo';

const completeName = `${name} ${surname}`;

function getGreeting(name) {
  return 'Hello ' + name;
}

console.log(`This is a text: ${getGreeting(name)}`);
